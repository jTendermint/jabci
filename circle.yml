dependencies:
  override:
    - mvn clean install dependency:resolve-plugins dependency:go-offline

deployment:
  main:
    branch: master
    commands:
      - mvn -P release -s .circleci.settings.xml -DskipTests deploy
